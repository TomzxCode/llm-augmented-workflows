name: Implementation

on:
  issues:
    types: [labeled]

permissions:
  contents: write
  pull-requests: write
  issues: write

jobs:
  call-implement-workflow:
    uses: TomzxCode/llm-augmented-workflows/.github/workflows/implement.yml@main
    secrets: inherit
