name: Plan Generation

on:
  issues:
    types: [opened, labeled]

permissions:
  contents: write
  pull-requests: write
  issues: write

jobs:
  call-plan-workflow:
    uses: TomzxCode/llm-augmented-workflows/.github/workflows/plan.yml@main
    secrets: inherit
